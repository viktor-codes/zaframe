# ZeeFrame API - deployment dependencies (pip)
# Locally recommended: uv sync (pyproject.toml + uv.lock).
# Keep this list aligned with [project].dependencies in pyproject.toml.
fastapi>=0.128.1
sqlalchemy>=2.0.46
uvicorn[standard]>=0.40.0
pydantic-settings>=2.0.0
pydantic[email]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
greenlet>=3.0.0
pyjwt>=2.13.0
resend>=2.0.0
stripe>=11.0.0
slowapi>=0.1.9
redis>=5.0.0
structlog>=25.5.0
prometheus-client>=0.25.0
sentry-sdk[fastapi]>=2.66.1
