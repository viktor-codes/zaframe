/** Session participants list + check-in / no-show (mobile-first). */
export { CheckInPanel, type CheckInPanelProps } from "./ui";
export {
  canCheckIn,
  canMarkNoShow,
  getParticipantDisplayName,
} from "./model/attendance-action";
