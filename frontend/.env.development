# Loaded automatically by `next dev` (NODE_ENV=development).
# Browser stays on the Next origin; rewrites proxy /api/* to FastAPI.
NEXT_PUBLIC_API_URL=http://localhost:3000
API_UPSTREAM_URL=http://127.0.0.1:8000
