# Пример .env.local для ZeeFrame Frontend
# Скопируйте в .env.local и при необходимости измените
# Для Vercel: добавьте NEXT_PUBLIC_API_URL в Environment Variables проекта (URL бэкенда)

# URL бэкенда (без trailing slash). Для production — URL вашего API (например https://api.example.com)
NEXT_PUBLIC_API_URL=http://localhost:8000
