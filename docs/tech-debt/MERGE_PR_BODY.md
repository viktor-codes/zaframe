# PR: `refactor/modular-monolith` → `main`

> Generated by TD-08 merge prep (2026-06-16). Paste body into GitHub PR.

## Summary

- **Modular monolith:** domain modules under `app/modules/`, shared ORM in `app/models/`, flat UoW (`uow.bookings`, `uow.occurrences`, …)
- **Architecture guards:** import-linter (7 contracts) + `tests/architecture/test_boundaries.py` (3 AST checks)
- **No breaking API route changes** — same paths and HTTP verbs; frontend aligned with Occurrence vocabulary
- **19 commits** on branch (scaffold → tech-debt follow-ups); narrative preserved for portfolio

### Commits (newest first)

| SHA | Message |
|-----|---------|
| `583d369` | docs: align README and retire stale architecture plan |
| `587c804` | refactor(occurrence): remove booking count from occurrence service |
| `f37ab1b` | refactor(booking): extract write persistence into booking/persistence |
| `4aba935` | refactor(catalog): split studio god-router across occurrence, public, schedule |
| `85da43e` | refactor(identity): add is_owned_by_user policy shared by booking and payment |
| `cc3697e` | refactor(payment/booking/catalog): split large services |
| `05df35c` | refactor(catalog): extract shared capacity logic into catalog/capacity |
| `787d3a9` | test(api): enforce module boundaries via import-linter + test_boundaries |
| `b91eca3` | refactor(api): add module router aggregator, slim main, drop facades |
| `6bb5767` | **efactor(booking)** — typo: missing `r`; see note below |
| `30c1ee5` | refactor(catalog): move public studio view into modules/catalog/public |
| `b48039e` | refactor(catalog): split service god-module into service/schedule |
| `f867a1f` | refactor(catalog): move studio into modules/catalog/studio |
| `e4bd671` | refactor(auth): move auth into modules/auth; email into integrations |
| `2e13316` | refactor(identity): extract identity (User) into modules/identity |
| `01198de` | refactor(payment): move payment + stripe webhooks into modules/payment |
| `3615231` | refactor(search): move search into modules/search |
| `c9a62fd` | docs(refactor): add modular-monolith agent task pack (tz-01..tz-11) |
| `2030fc6` | chore(api): scaffold modular-monolith refactor (ADR-003, import-linter) |

> **Commit typo:** `6bb5767` reads `efactor(booking)` instead of `refactor(booking)`. Branch has **no remote upstream** yet — safe to reword locally before first push if desired. Do **not** force-push after publish without explicit approval.

## TD-08 quality gates (2026-06-16)

| Gate | Result |
|------|--------|
| `uv run ruff check .` | ✅ pass (after 2 lint fixes in tests — see uncommitted changes) |
| `uv run lint-imports` | ✅ **7 contracts kept**, 0 broken (117 files, 365 deps) |
| `uv run pytest -q` | ✅ **182 passed** |
| `npm run build` (frontend) | ✅ pass (Next.js 16.1.6) |
| `npm run test` (vitest) | ✅ **13 passed** (2 files) |

### Branch hygiene

| Check | Result |
|-------|--------|
| `.env` / secrets in diff | ✅ none |
| `uv.lock` in diff | ⚠️ `+191` lines (import-linter + deps — intentional) |
| Diff vs `origin/main` | 218 files, +12923 / −3899 lines |
| Remote tracking | Branch **not pushed** (`no upstream configured`) |

## Test plan

- [x] `uv run pytest -q` (182 passed)
- [x] `uv run lint-imports` (7 kept)
- [ ] Manual smoke: studio public → book → OTP → pay (Stripe test)
- [ ] OpenAPI `/docs` — verify paths unchanged

## ADR

- [docs/adr/003-modular-monolith.md](../adr/003-modular-monolith.md)

## Follow-up (separate PRs)

- [docs/tech-debt/README.md](./README.md) — P1 items (td-01 … td-07)
- td-09: Pyright in CI
- td-10: E2E book-pay flow (Playwright)
- td-11: booking lifecycle cron hardening

## Merge strategy (recommendation)

**Use merge commit** (or rebase-merge) to preserve the 19-step refactor narrative for portfolio.

**Do not** squash the entire branch into one commit unless explicitly requested — it loses the step-by-step story.

---

*Awaiting user approval of merge strategy before merging to `main`.*
